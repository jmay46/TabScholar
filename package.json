{
  "name": "tabscholar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A local-first study workflow extension for user-provided material.",
  "scripts": {
    "start": "node companion/server.mjs",
    "preview": "node scripts/preview.mjs",
    "icons": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/generate-icons.ps1",
    "check": "node scripts/check.mjs",
    "test": "node --test",
    "verify": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
